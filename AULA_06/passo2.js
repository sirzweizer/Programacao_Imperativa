let cores = ['cinza', 'vermelho', 'verde', 'azul'];

//crie um array, percorra, altere e exiba seus elementos
console.log("Percorrendo os elementos do array Cores:")
for(let i=0;i<cores.length;i++)
{
    console.log(cores[i]);
}

console.log ("Vamos alterar as tonalidades...")
cores = ['cinza escuro','vermelho escuro', 'verde escuro', 'azul escuro'];

console.log("Verificando o array cores com as novas tonalidades:")
for(let i=0;i<cores.length;i++)
{
    console.log(cores[i]);
}
